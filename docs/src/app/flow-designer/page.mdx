import { PageHeader } from '@/components/chakra/PageHeader'
import { Prose } from '@/components/chakra/Prose'
import { Card, Code, VStack, Text, Heading, Box, Alert, Badge, HStack, Separator, SimpleGrid, Blockquote } from '@chakra-ui/react'
import { SimpleCodeBlock } from '@/components/chakra/SimpleCodeBlock'
import { DocLinkCard } from '@/components/chakra/DocLinkCard'
export const metadata = {
  title: 'Flow Designer',
  description: 'Visual tool for designing and testing verification flows in the FaceSign Sandbox.',
}

<PageHeader title="Flow Designer" description="Design and test verification flows visually before implementing with the API." />

<Prose>

## What is Flow Designer?

Flow Designer is a **visual design and testing tool** for creating identity verification workflows. It runs in the FaceSign Sandbox environment and is intended for:

- ✅ **Prototyping** verification flows
- ✅ **Testing** flow logic with sample data
- ✅ **Creating demos** for stakeholders
- ❌ **NOT for production use**
- ❌ **NOT for real user data or PII**

<Alert.Root status="warning" variant="subtle" my={6}>
  <Alert.Indicator />
  <Alert.Content>
    <Alert.Title>Sandbox Environment Only</Alert.Title>
    <Alert.Description>
      Flow Designer is part of the FaceSign Sandbox. Never input real user data, actual identity documents,
      or any personally identifiable information. Use test data and sample identities only.
    </Alert.Description>
  </Alert.Content>
</Alert.Root>

## Getting Access

To use Flow Designer, you need access to the FaceSign Sandbox:

1. **Request Access**: Contact your FaceSign administrator for provisioning
2. **Receive Credentials**: You'll get access to the Sandbox environment
3. **Access Flow Designer**: Navigate to https://create.facesign.ai
4. **Find Your API Key**: Once logged in, go to Settings → API Keys

Your Sandbox API key will look like: `sk_test_...`

![API Key Location - Shows where to find your test API key in Settings](/images/flow_designer_api_key.png)

<Separator my={8} />

## Creating Flows Visually

Flow Designer provides a drag-and-drop interface for building verification flows:

### Node Library

The left panel contains all available verification nodes:

<div>
<SimpleGrid columns={{ base: 1, sm: 2, md: 3, lg: 4 }} gap={3} mt={4} mb={6}>
  <Card.Root
    variant="outline"
    borderWidth="1px"
    borderColor="gray.200"
    bg="white"
    boxShadow="none"
    _hover={{ borderColor: "gray.400", bg: "gray.50" }}
    transition="all 0.15s"
    size="sm"
  >
    <Card.Body p={4}>
      <VStack align="start" gap={2}>
        <Heading as="h4" size="xs" fontWeight="semibold">Conversation</Heading>
        <Text fontSize="xs" color="gray.600" lineHeight="1.5">
          AI-powered conversational interactions
        </Text>
      </VStack>
    </Card.Body>
  </Card.Root>

  <Card.Root
    variant="outline"
    borderWidth="1px"
    borderColor="gray.200"
    bg="white"
    boxShadow="none"
    _hover={{ borderColor: "gray.400", bg: "gray.50" }}
    transition="all 0.15s"
    size="sm"
  >
    <Card.Body p={4}>
      <VStack align="start" gap={2}>
        <Heading as="h4" size="xs" fontWeight="semibold">Document Scan</Heading>
        <Text fontSize="xs" color="gray.600" lineHeight="1.5">
          Identity document verification
        </Text>
      </VStack>
    </Card.Body>
  </Card.Root>

  <Card.Root
    variant="outline"
    borderWidth="1px"
    borderColor="gray.200"
    bg="white"
    boxShadow="none"
    _hover={{ borderColor: "gray.400", bg: "gray.50" }}
    transition="all 0.15s"
    size="sm"
  >
    <Card.Body p={4}>
      <VStack align="start" gap={2}>
        <Heading as="h4" size="xs" fontWeight="semibold">Liveness Detection</Heading>
        <Text fontSize="xs" color="gray.600" lineHeight="1.5">
          Anti-spoofing checks
        </Text>
      </VStack>
    </Card.Body>
  </Card.Root>

  <Card.Root
    variant="outline"
    borderWidth="1px"
    borderColor="gray.200"
    bg="white"
    boxShadow="none"
    _hover={{ borderColor: "gray.400", bg: "gray.50" }}
    transition="all 0.15s"
    size="sm"
  >
    <Card.Body p={4}>
      <VStack align="start" gap={2}>
        <Heading as="h4" size="xs" fontWeight="semibold">Data Collect</Heading>
        <Text fontSize="xs" color="gray.600" lineHeight="1.5">
          Data collection and input
        </Text>
      </VStack>
    </Card.Body>
  </Card.Root>

  <Card.Root
    variant="outline"
    borderWidth="1px"
    borderColor="gray.200"
    bg="white"
    boxShadow="none"
    _hover={{ borderColor: "gray.400", bg: "gray.50" }}
    transition="all 0.15s"
    size="sm"
  >
    <Card.Body p={4}>
      <VStack align="start" gap={2}>
        <Heading as="h4" size="xs" fontWeight="semibold">Face Scan</Heading>
        <Text fontSize="xs" color="gray.600" lineHeight="1.5">
          Facial recognition/matching
        </Text>
      </VStack>
    </Card.Body>
  </Card.Root>

  <Card.Root
    variant="outline"
    borderWidth="1px"
    borderColor="gray.200"
    bg="white"
    boxShadow="none"
    _hover={{ borderColor: "gray.400", bg: "gray.50" }}
    transition="all 0.15s"
    size="sm"
  >
    <Card.Body p={4}>
      <VStack align="start" gap={2}>
        <Heading as="h4" size="xs" fontWeight="semibold">Two Factor</Heading>
        <Text fontSize="xs" color="gray.600" lineHeight="1.5">
          SMS/Email OTP verification
        </Text>
      </VStack>
    </Card.Body>
  </Card.Root>

  <Card.Root
    variant="outline"
    borderWidth="1px"
    borderColor="gray.200"
    bg="white"
    boxShadow="none"
    _hover={{ borderColor: "gray.400", bg: "gray.50" }}
    transition="all 0.15s"
    size="sm"
  >
    <Card.Body p={4}>
      <VStack align="start" gap={2}>
        <Heading as="h4" size="xs" fontWeight="semibold">Data Validation</Heading>
        <Text fontSize="xs" color="gray.600" lineHeight="1.5">
          Conditional logic nodes
        </Text>
      </VStack>
    </Card.Body>
  </Card.Root>
</SimpleGrid>
</div>

![Empty Canvas - Shows the blank Flow Designer workspace with node library on the left](/images/flow_designer_empty_flow.png)

### Building Your Flow

1. **Drag nodes** from the library onto the canvas
2. **Connect nodes** by dragging from output to input ports
3. **Configure each node** by clicking to open properties panel
4. **Set conditions** for branching logic

![Connecting Nodes - Shows drawing an edge between two nodes](/images/flow_designer_connect_node.png)

### Configuring Nodes

Click any node to configure its properties:

<Box my={6} p={4} bg="gray.50" borderRadius="md">
  <Text fontWeight="bold" mb={2}>📍 SCREENSHOT PLACEHOLDER</Text>
  <Text fontSize="sm" color="gray.600">[Node Configuration - Shows the properties panel for a Document Scan node with options for document types, regions, etc.]</Text>
</Box>

<Separator my={8} />

## Exporting Flows (BETA)

Once you've designed and tested your flow, you can export it for use with the production API:

### Export Process

1. Open your tested flow in Flow Designer
2. Click the **Export** button in the toolbar
3. Choose export format (currently JSON only)
4. Copy the exported configuration

<Box my={6} p={4} bg="gray.50" borderRadius="md">
  <Text fontWeight="bold" mb={2}>📍 SCREENSHOT PLACEHOLDER</Text>
  <Text fontSize="sm" color="gray.600">[Export Button - Shows the Export button in the Flow Designer toolbar]</Text>
</Box>

### Exported Flow Format

The exported JSON contains your complete flow configuration:

<SimpleCodeBlock
  title="Example Exported Flow"
  language="json"
  code={`{
  "version": "1.0",
  "nodes": [
    {
      "id": "start_1",
      "type": "start"
    },
    {
      "id": "email_1",
      "type": "enter_email",
      "config": {
        "required": true,
        "validation": "email"
      },
      "outcomes": {
        "emailEntered": "doc_1",
        "canceled": "end_1"
      }
    },
    {
      "id": "doc_1",
      "type": "document_scan",
      "config": {
        "documentTypes": ["passport", "driver_license", "id_card"],
        "regions": ["US", "CA", "EU"],
        "requireLiveness": true
      },
      "outcomes": {
        "scanSuccess": "end_1",
        "userCancelled": "end_1",
        "timeout": "end_1"
      }
    },
    {
      "id": "end_1",
      "type": "end"
    }
  ],
  "edges": [
    { "id": "e1", "source": "start_1", "target": "email_1" },
    { "id": "e2", "source": "email_1", "target": "doc_1", "outcome": "emailEntered" },
    { "id": "e3", "source": "email_1", "target": "end_1", "outcome": "canceled" },
    { "id": "e4", "source": "doc_1", "target": "end_1" }
  ]
}`}
/>

<Blockquote.Root variant="subtle" colorPalette="orange" my={6}>
  <Blockquote.Content>
    <Text fontWeight="semibold">Beta Feature Notice</Text>
    <Text>
      Flow export is currently in beta. The exported format may change in future releases.
      Always validate and test exported flows before using in production.
    </Text>
  </Blockquote.Content>
</Blockquote.Root>

![Export Dialog - Shows the export dialog with JSON output and copy functionality](/images/flow_designer_export_flow.png)

<Separator my={8} />

## Next Steps: Implement with the API

After designing and exporting your flow, implement it in your application:

### 1. Install the SDK

<SimpleCodeBlock
  defaultLanguage="npm"
  languageSwitcher="tabs"
  codeExamples={{
    npm: "npm install @facesignai/api",
    yarn: "yarn add @facesignai/api",
    python: "pip install facesignai",
  }}
/>

### 2. Use Your Exported Flow

<SimpleCodeBlock
  title="Implement in Production"
  language="javascript"
  code={`import { Client } from '@facesignai/api'
import flowConfig from './exported-flow.json'

const client = new Client({
  auth: process.env.FACESIGN_LIVE_KEY, // Production key: sk_live_...
  serverUrl: 'https://api.facesign.ai'
})

// Create a session with your exported flow
const { session, clientSecret } = await client.session.create({
  clientReferenceId: userId,
  metadata: { source: 'onboarding' },
  flow: flowConfig  // Your exported flow configuration
})

// Direct user to verification
console.log('Verification URL:', clientSecret.url)`}
/>

### 3. Handle Production Data

Remember the key difference:

- **Sandbox (Flow Designer)**: Test data only, no PII
- **Production (Your App)**: Real user data, full security

<Alert.Root status="success" variant="subtle" my={6}>
  <Alert.Indicator />
  <Alert.Content>
    <Alert.Title>Ready for Production</Alert.Title>
    <Alert.Description>
      Your flow is now designed, tested, and ready to be implemented in your production environment
      where you control all user data.
    </Alert.Description>
  </Alert.Content>
</Alert.Root>

## Common Flow Patterns

Here are some common verification flow patterns you can create in Flow Designer:

<SimpleGrid columns={{ base: 1, md: 3 }} gap={4} mt={6}>
  <Card.Root
    variant="outline"
    borderWidth="1px"
    borderColor="gray.200"
    bg="white"
    boxShadow="none"
    _hover={{ borderColor: "gray.300", boxShadow: "sm" }}
    transition="all 0.2s"
  >
    <Card.Body p={5}>
      <HStack justify="space-between" mb={3}>
        <Heading as="h4" size="sm" fontWeight="semibold">
          Basic KYC
        </Heading>
        <Badge size="sm" variant="subtle" colorPalette="gray">
          Standard
        </Badge>
      </HStack>

      <Box
        bg="gray.50"
        p={3}
        borderRadius="md"
        fontSize="xs"
        fontFamily="mono"
        color="gray.700"
        lineHeight="1.6"
      >
        Start → Email Entry → Document Scan → Liveness Check → End
      </Box>

      <Text fontSize="sm" color="gray.600" mt={3}>
        Standard identity verification flow for most use cases.
      </Text>
    </Card.Body>
  </Card.Root>

  <Card.Root
    variant="outline"
    borderWidth="1px"
    borderColor="gray.200"
    bg="white"
    boxShadow="none"
    _hover={{ borderColor: "gray.300", boxShadow: "sm" }}
    transition="all 0.2s"
  >
    <Card.Body p={5}>
      <HStack justify="space-between" mb={3}>
        <Heading as="h4" size="sm" fontWeight="semibold">
          Enhanced Security
        </Heading>
        <Badge size="sm" variant="subtle" colorPalette="gray">
          Advanced
        </Badge>
      </HStack>

      <Box
        bg="gray.50"
        p={3}
        borderRadius="md"
        fontSize="xs"
        fontFamily="mono"
        color="gray.700"
        lineHeight="1.6"
      >
        Start → Email Entry → Document Scan → Face Match → Two Factor Auth → End
      </Box>

      <Text fontSize="sm" color="gray.600" mt={3}>
        High-security verification with multiple verification layers.
      </Text>
    </Card.Body>
  </Card.Root>

  <Card.Root
    variant="outline"
    borderWidth="1px"
    borderColor="gray.200"
    bg="white"
    boxShadow="none"
    _hover={{ borderColor: "gray.300", boxShadow: "sm" }}
    transition="all 0.2s"
  >
    <Card.Body p={5}>
      <HStack justify="space-between" mb={3}>
        <Heading as="h4" size="sm" fontWeight="semibold">
          Progressive
        </Heading>
        <Badge size="sm" variant="subtle" colorPalette="gray">
          Risk-Based
        </Badge>
      </HStack>

      <Box
        bg="gray.50"
        p={3}
        borderRadius="md"
        fontSize="xs"
        fontFamily="mono"
        color="gray.700"
        lineHeight="1.6"
      >
        Start → Email Entry → Data Validation → (high risk) → Document | (low risk) → End
      </Box>

      <Text fontSize="sm" color="gray.600" mt={3}>
        Risk-based verification with conditional branching.
      </Text>
    </Card.Body>
  </Card.Root>
</SimpleGrid>

## Limitations and Considerations

### Sandbox Limitations

- **Rate limits**: Lower than production
- **Data retention**: Test data cleared regularly
- **Features**: Some production features may not be available
- **Performance**: Not representative of production performance

### Production Considerations

When moving to production:

- Implement proper error handling
- Add monitoring and logging
- Set up webhook handlers
- Configure retry logic
- Plan for scale

## Support and Resources

<SimpleGrid columns={{ base: 1, md: 3 }} gap={4} mt={8}>
  <DocLinkCard
    href="/architecture"
    title="Architecture Overview"
    description="Understand the Sandbox vs Production model"
  />
  <DocLinkCard
    href="/flows/node-reference"
    title="Node Reference"
    description="Detailed documentation for each node type"
  />
  <DocLinkCard
    href="/api"
    title="API Reference"
    description="Complete API documentation"
  />
</SimpleGrid>

</Prose>