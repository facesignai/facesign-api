import { Libraries } from '@/components/Libraries'

export const metadata = {
  title: 'FaceSign SDKs',
  description:
    'Official SDKs and client libraries for integrating FaceSign identity verification into your applications.',
}

export const sections = [
  { title: 'Available SDKs', id: 'official-libraries' },
  { title: 'Planned SDKs', id: 'upcoming-libraries' },
]

# FaceSign SDKs

The recommended way to interact with the FaceSign API is by using our official TypeScript/JavaScript SDK. We're actively developing SDKs for additional languages based on community demand and feedback. {{ className: 'lead' }}

<Libraries />
