export const metadata = {
  title: 'Client Secret (moved)',
  description: 'This page has moved to /docs/sessions#client-secret.',
}

import { redirect } from 'next/navigation'

{redirect('/docs/sessions#client-secret')}