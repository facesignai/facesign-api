export const metadata = {
  title: 'Session Object (moved)',
  description: 'This page has moved to /docs/sessions#session-object.',
}

import { redirect } from 'next/navigation'

{redirect('/docs/sessions#session-object')}